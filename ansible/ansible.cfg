[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
stdout_callback = yaml
nocows = True
# Fact caching isn't needed for a single local run, but this keeps output tidy.
display_skipped_hosts = True
